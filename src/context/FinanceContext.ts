// This file exists to maintain backward compatibility with existing imports
// All components should gradually migrate to importing from '@/hooks/useFinance' directly

export { useFinance } from '@/hooks/useFinance';
export type { TransactionFilterType } from '@/hooks/useFinance'; 